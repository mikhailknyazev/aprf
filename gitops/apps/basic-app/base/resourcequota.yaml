apiVersion: v1
kind: ResourceQuota
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "60100"
  name: basic-app-quota
spec:
  hard:
    limits.cpu: "1"
    limits.memory: 1Gi
    pods: "10"
    requests.cpu: 500m
    requests.memory: 512Mi
