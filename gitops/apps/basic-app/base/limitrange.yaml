apiVersion: v1
kind: LimitRange
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "60100"
  name: basic-app-defaults
spec:
  limits:
    - default:
        cpu: 250m
        memory: 256Mi
      defaultRequest:
        cpu: 50m
        memory: 128Mi
      type: Container
